type: object
properties:
  token:
    type: string
    description: JWT Token
  expiresIn:
    type: integer
    description: Token过期时间(秒)
  isNewUser:
    type: boolean
    description: 是否为新注册用户
  user:
    $ref: ./UserVO.yaml
